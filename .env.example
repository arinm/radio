# Database (PostgreSQL for production, SQLite for local dev)
DATABASE_URL="file:./dev.db"
# DATABASE_URL="postgresql://user:password@localhost:5432/radio_online?schema=public"

# Application
NEXT_PUBLIC_SITE_URL="https://radioonline.ro"
NEXT_PUBLIC_SITE_NAME="Radio Online Romania"

# Security
RATE_LIMIT_MAX_REQUESTS="60"
RATE_LIMIT_WINDOW_MS="60000"

# Analytics (loaded only after consent)
# NEXT_PUBLIC_GA_MEASUREMENT_ID=""

# Optional: Sentry (configured as necessary-only, no PII)
# SENTRY_DSN=""
# NEXT_PUBLIC_SENTRY_DSN=""

# Optional: Redis for caching
# REDIS_URL="redis://localhost:6379"

# Admin (future use)
# ADMIN_SECRET=""
